import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#80E3DB', 
        },
        secondary: {
            main: '#F69722', 
        },
    },
});

export default theme;
