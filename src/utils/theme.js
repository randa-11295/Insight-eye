import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#36938d', 
        },
        secondary: {
            main: '#F69722', 
        },
    },
});

export default theme;
